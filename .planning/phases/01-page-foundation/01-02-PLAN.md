---
phase: 01-page-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/modules/scroll-spy.js
  - src/modules/nav-toggle.js
  - src/main.js
autonomous: false
requirements:
  - VIZ-03

must_haves:
  truths:
    - "Clicking a nav link smoothly scrolls to the target section without jumps or jank"
    - "The active nav link highlights to match the currently visible section while scrolling"
    - "On mobile, tapping the hamburger button opens the nav menu; tapping a link closes it and scrolls to the section"
    - "Users with prefers-reduced-motion enabled see instant jumps instead of smooth scroll"
  artifacts:
    - path: "src/modules/scroll-spy.js"
      provides: "IntersectionObserver-based active nav highlighting"
      exports: ["initScrollSpy"]
    - path: "src/modules/nav-toggle.js"
      provides: "Mobile hamburger menu toggle with aria-expanded"
      exports: ["initNavToggle"]
  key_links:
    - from: "src/main.js"
      to: "src/modules/scroll-spy.js"
      via: "import and init call"
      pattern: "import.*initScrollSpy"
    - from: "src/main.js"
      to: "src/modules/nav-toggle.js"
      via: "import and init call"
      pattern: "import.*initNavToggle"
    - from: "src/modules/scroll-spy.js"
      to: "index.html"
      via: "querySelector for section[id] and nav a[href^='#']"
      pattern: "querySelectorAll.*section\\[id\\]"
    - from: "src/modules/nav-toggle.js"
      to: "index.html"
      via: "querySelector for .nav-toggle and .nav-menu"
      pattern: "querySelector.*nav-toggle"
---

<objective>
Add smooth scroll navigation interactivity -- scroll spy that highlights the active nav link, and mobile hamburger toggle -- then visually verify the complete page.

Purpose: Complete VIZ-03 (smooth scroll navigation) by adding the JavaScript modules that make the nav interactive. CSS smooth scrolling was set up in Plan 01; this plan adds the scroll spy and mobile nav toggle that make the navigation feel polished.

Output: Two JS modules (scroll-spy, nav-toggle) wired into main.js, plus human verification that the complete page foundation works correctly.
</objective>

<execution_context>
@C:/Users/andre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-page-foundation/01-RESEARCH.md
@.planning/phases/01-page-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - HTML structure the JS modules will target -->

From index.html (Plan 01 creates):
- nav with class="main-nav" and aria-label="Main navigation"
- button with class="nav-toggle", aria-expanded="false", aria-controls="nav-menu"
- ul with id="nav-menu" and class="nav-menu"
- nav links: a[href="#hero"], a[href="#expertise"], a[href="#contact"]
- sections: section#hero, section#expertise, section#contact

From src/styles/nav.css (Plan 01 creates):
- .nav-menu.is-open — class that shows the mobile menu
- .active — class on nav links for active state (styled with accent-cyan color)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scroll spy and mobile nav toggle modules</name>
  <files>src/modules/scroll-spy.js, src/modules/nav-toggle.js, src/main.js</files>
  <action>
Create two JavaScript modules following the patterns from 01-RESEARCH.md exactly.

**src/modules/scroll-spy.js** -- IntersectionObserver scroll spy (RESEARCH Pattern 3):
```javascript
export function initScrollSpy() {
  const sections = document.querySelectorAll('section[id]');
  const navLinks = document.querySelectorAll('nav a[href^="#"]');

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          navLinks.forEach((link) => {
            link.classList.toggle(
              'active',
              link.getAttribute('href') === `#${entry.target.id}`
            );
          });
        }
      });
    },
    {
      rootMargin: '-20% 0px -80% 0px',
      threshold: 0,
    }
  );

  sections.forEach((section) => observer.observe(section));
}
```

**src/modules/nav-toggle.js** -- Mobile hamburger toggle (RESEARCH Pattern 5):
```javascript
export function initNavToggle() {
  const toggle = document.querySelector('.nav-toggle');
  const menu = document.querySelector('.nav-menu');

  if (!toggle || !menu) return;

  toggle.addEventListener('click', () => {
    const isOpen = toggle.getAttribute('aria-expanded') === 'true';
    toggle.setAttribute('aria-expanded', String(!isOpen));
    menu.classList.toggle('is-open');
  });

  // Close menu when a nav link is clicked (for smooth scroll to section)
  menu.querySelectorAll('a').forEach((link) => {
    link.addEventListener('click', () => {
      toggle.setAttribute('aria-expanded', 'false');
      menu.classList.remove('is-open');
    });
  });
}
```

**src/main.js** -- Add imports and initialization (append to existing CSS imports from Plan 01):
Add these imports at the top (after CSS imports):
```javascript
import { initScrollSpy } from './modules/scroll-spy.js';
import { initNavToggle } from './modules/nav-toggle.js';
```

Add initialization on DOMContentLoaded:
```javascript
document.addEventListener('DOMContentLoaded', () => {
  initScrollSpy();
  initNavToggle();
});
```

Do NOT remove the existing CSS imports from Plan 01 -- only append to main.js.

After implementation, run `npm test` to verify all existing tests still pass.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Two JS modules created and wired into main.js. All existing tests still pass. Scroll spy and nav toggle code follows the exact patterns from research. Functions are exported and initialized on DOMContentLoaded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete page foundation</name>
  <action>
Human verifies the complete Phase 1 page foundation: dark-themed responsive landing page with smooth scroll navigation, scroll spy highlighting, and mobile hamburger menu.

Steps:
1. Run `npm run dev` if not already running
2. Open the page in browser at the localhost URL shown in terminal

**Dark theme (VIZ-02):**
3. Verify the page has a dark background (#0a0a0f-ish), light text, and cyan/purple accent colors
4. Verify the hero section shows "Elmar" heading with a subtle cyan glow effect
5. Verify the expertise section has a different dark shade and a neon gradient line at the top

**Smooth scroll navigation (VIZ-03):**
6. Click "Expertise" in the nav bar -- page should smoothly scroll to the expertise section
7. Click "Contact" -- smooth scroll to contact section
8. Click "Home" -- smooth scroll back to hero
9. While scrolling manually, verify the active nav link highlights (changes to cyan) to match the visible section

**Responsive layout (VIZ-04):**
10. Open browser DevTools, toggle device toolbar (Ctrl+Shift+M)
11. Set width to 375px -- verify: hamburger menu appears, content stacks vertically, no horizontal overflow
12. Tap the hamburger -- verify: nav menu opens, links visible
13. Tap a nav link -- verify: menu closes, page scrolls to section
14. Set width to 768px -- verify: layout adjusts, potentially more padding
15. Set width to 1440px -- verify: content centered, max-width constraint, full desktop nav visible

**Expected overall impression:** A polished, futuristic dark page that feels complete as a layout foundation (placeholder content in expertise/contact is fine since Phase 2 and 3 will fill those).
  </action>
  <verify>User types "approved" or describes issues to fix.</verify>
  <done>Human has visually confirmed dark theme, smooth scroll navigation, scroll spy active states, responsive layout at all breakpoints, and mobile hamburger menu functionality.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `npm test` -- all tests from Plan 01 still pass
2. Human has verified: dark theme, smooth scroll, responsive layout, scroll spy, mobile nav toggle
3. VIZ-03 (smooth scroll navigation) requirement fully addressed
</verification>

<success_criteria>
- Scroll spy correctly highlights active nav link based on visible section
- Mobile hamburger toggle opens/closes nav menu with proper aria-expanded state
- Clicking nav links smoothly scrolls to sections on both desktop and mobile
- Nav menu closes after link click on mobile
- All previously passing tests remain green
- Human has visually approved the complete page foundation
</success_criteria>

<output>
After completion, create `.planning/phases/01-page-foundation/01-02-SUMMARY.md`
</output>
